---
phase: quick-tasks
task: supabase-auth
total_tasks: 3
status: not_started
last_updated: 2026-03-02T06:21:19.456Z
---

<current_state>
Completed 2 quick tasks (voice selection + player UX overhaul) and a full debug audit.
Next task: Build Supabase auth integration from scratch. User provided their Supabase project: https://supabase.com/dashboard/project/upuflykybtvdgzsqzzon (project ref: upuflykybtvdgzsqzzon).
</current_state>

<completed_work>

- Quick Task 1: Added 5 selectable human-sounding voices (Ava, Emma, Andrew, Sonia, Brian) with voice picker UI and voice preview on tap
- Quick Task 2: Player UX overhaul — CrossfadeAudioPair for smooth looping, one-at-a-time affirmation display, Mindi upper-third + translucent, 25-30 personalized affirmations with user name, home ambient music, glassmorphic controls, vignette, shooting stars, staggered entrance, session celebration
- Added 10 background tracks (was 5): 3 lofi + 4 ambient + 3 nature, all regenerated at 128kbps
- Debug audit: Fixed 6 bugs — CrossfadeAudioPair stale ref (root cause of TTS cutout), short audio fallback, HomeAmbientPlayer iOS background stomping, ambient race condition, AffirmationSpirals textShadowRadius crash
- Confirmed: Supabase auth does NOT exist in codebase — zero references. Needs full build.
</completed_work>

<remaining_work>

- Supabase Auth Integration (NOT STARTED):
  - Install @supabase/supabase-js
  - Create Supabase client config (need SUPABASE_URL + SUPABASE_ANON_KEY from user's project upuflykybtvdgzsqzzon)
  - Build auth screens: Sign Up, Sign In, Forgot Password
  - Build user profiles table in Supabase
  - Protect routes (redirect to auth if not logged in)
  - Connect userName from store to Supabase profile
  - Add logout functionality
  - Persist session with AsyncStorage
- User needs to provide: SUPABASE_URL and SUPABASE_ANON_KEY from their dashboard (Settings > API)
</remaining_work>

<decisions_made>

- Voice selection decoupled from soundtrack (voices are independent of tracks)
- CrossfadeAudioPair uses dual Audio.Sound instances with 3s-before-end detection
- Affirmations redesigned to show one at a time in lower half, Mindi in upper third at 0.7 opacity
- Home ambient uses ocean-waves at 0.15 volume, stops when entering player
- Groq generates 25-30 affirmations with user name in ~30%, TTS shuffles/repeats to ~30 items
- All ambient tracks are 5min/128kbps, generated with FFmpeg synthesis
</decisions_made>

<blockers>
- Need Supabase project URL and anon key from user (Settings > API in dashboard)
- Cannot access Supabase dashboard directly (requires auth)
</blockers>

<context>
User wants full auth: login, signup, logout, user profiles. Their Supabase project ref is upuflykybtvdgzsqzzon.
The app currently has no auth at all — the store has userName/userId fields but they're just local state.
The onboarding flow exists (name-mindi screen) but has no real auth.
Need to ask user for their Supabase URL + anon key when resuming, then build the full auth flow.
</context>

<next_action>
1. Ask user for Supabase URL and anon key (from dashboard Settings > API)
2. Run /gsd:quick to build Supabase auth integration: client setup, auth screens, profile table, route protection, logout
</next_action>
