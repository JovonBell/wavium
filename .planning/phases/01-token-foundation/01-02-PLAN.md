---
phase: 01-token-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wavium/app.json
  - wavium/src/theme/typography.ts
  - wavium/src/theme/index.ts
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04
  - TYPO-05
  - TYPO-06

must_haves:
  truths:
    - "Three Google Fonts packages are installed in node_modules"
    - "expo-font config plugin is registered in app.json with 6 font file paths"
    - "fontFamilies constant is exported from typography.ts with 6 named font constants"
    - "textStyles map includes displayHero, displayHeading, affirmation, body, label, button variants"
    - "No useFonts() call added anywhere — config plugin approach only"
  artifacts:
    - path: "wavium/app.json"
      provides: "expo-font config plugin registration with 6 font paths"
      contains: "expo-font"
    - path: "wavium/src/theme/typography.ts"
      provides: "fontFamilies constant and updated textStyles"
      exports: ["fontFamilies", "textStyles"]
    - path: "wavium/src/theme/index.ts"
      provides: "fontFamilies re-export"
      contains: "fontFamilies"
  key_links:
    - from: "wavium/app.json"
      to: "node_modules/@expo-google-fonts/cinzel/Cinzel_400Regular.ttf"
      via: "expo-font config plugin fonts array"
      pattern: "Cinzel_400Regular"
    - from: "wavium/src/theme/typography.ts"
      to: "fontFamilies"
      via: "named font constants matching embedded font names"
      pattern: "Cinzel_400Regular|CormorantGaramond|Raleway"
---

<objective>
Install the three Google Fonts packages (Cinzel, Cormorant Garamond, Raleway), register them via the expo-font config plugin in app.json for build-time embedding, and extend typography.ts with fontFamilies constants and updated textStyles — establishing the typography token system.

Purpose: Fonts must be embedded at build time (zero font flash on cold start) and referenced via named constants so every screen and component can use the correct typeface without fontWeight approximations.
Output: app.json with expo-font plugin, typography.ts with fontFamilies + textStyles, fonts installable via npx expo install.
</objective>

<execution_context>
@/Users/joshuabellhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuabellhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joshuabellhome/wavium/.planning/ROADMAP.md
@/Users/joshuabellhome/wavium/.planning/STATE.md
@/Users/joshuabellhome/wavium/.planning/phases/01-token-foundation/01-RESEARCH.md
@/Users/joshuabellhome/wavium/wavium/app.json
@/Users/joshuabellhome/wavium/wavium/src/theme/typography.ts
@/Users/joshuabellhome/wavium/wavium/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install font packages and register expo-font config plugin</name>
  <files>wavium/app.json</files>
  <action>
Step 1 — Install the three font packages from the wavium/ app directory:
```bash
cd /Users/joshuabellhome/wavium/wavium && npx expo install @expo-google-fonts/cinzel @expo-google-fonts/cormorant-garamond @expo-google-fonts/raleway
```

Step 2 — Verify exact font filenames after install:
```bash
ls /Users/joshuabellhome/wavium/wavium/node_modules/@expo-google-fonts/cinzel/*.ttf
ls /Users/joshuabellhome/wavium/wavium/node_modules/@expo-google-fonts/cormorant-garamond/*.ttf
ls /Users/joshuabellhome/wavium/wavium/node_modules/@expo-google-fonts/raleway/*.ttf
```

Step 3 — Read the current app.json, then add the expo-font plugin to the plugins array. The existing plugins array has "expo-router" — add expo-font as the second entry:

```json
"plugins": [
  "expo-router",
  [
    "expo-font",
    {
      "fonts": [
        "./node_modules/@expo-google-fonts/cinzel/Cinzel_400Regular.ttf",
        "./node_modules/@expo-google-fonts/cinzel/Cinzel_700Bold.ttf",
        "./node_modules/@expo-google-fonts/cormorant-garamond/CormorantGaramond_400Regular.ttf",
        "./node_modules/@expo-google-fonts/cormorant-garamond/CormorantGaramond_400Regular_Italic.ttf",
        "./node_modules/@expo-google-fonts/raleway/Raleway_400Regular.ttf",
        "./node_modules/@expo-google-fonts/raleway/Raleway_500Medium.ttf"
      ]
    }
  ]
]
```

IMPORTANT: Use the exact .ttf filenames confirmed from the ls commands in Step 2. If the italic variant is named differently (e.g., CormorantGaramond_400Italic.ttf instead of CormorantGaramond_400Regular_Italic.ttf), use the actual filename from disk.

Do NOT add useFonts() to _layout.tsx. The config plugin embeds fonts at build time — no runtime loading is needed. The existing SplashScreen pattern in _layout.tsx is already correct and requires no changes.
  </action>
  <verify>
    <automated>ls /Users/joshuabellhome/wavium/wavium/node_modules/@expo-google-fonts/cinzel/Cinzel_400Regular.ttf && grep -c "expo-font" /Users/joshuabellhome/wavium/wavium/app.json</automated>
    <manual>Confirm app.json has expo-font plugin with 6 font paths. Confirm all 6 .ttf paths point to files that exist in node_modules.</manual>
  </verify>
  <done>All 3 font packages installed in node_modules. app.json plugins array contains expo-font entry with 6 font file paths. All 6 paths resolve to existing .ttf files.</done>
</task>

<task type="auto">
  <name>Task 2: Add fontFamilies constant and update textStyles in typography.ts</name>
  <files>wavium/src/theme/typography.ts, wavium/src/theme/index.ts</files>
  <action>
Read the current typography.ts file first to understand existing exports (fontSizes, textStyles, etc.). Then make the following changes:

**In typography.ts:**

Add the fontFamilies constant near the top of the file, after any imports but before the existing constants:
```typescript
import { TextStyle } from 'react-native';

export const fontFamilies = {
  // Display — Roman inscription gravity (TYPO-01)
  displayRegular: 'Cinzel_400Regular',
  displayBold: 'Cinzel_700Bold',

  // Editorial serif — luxury affirmations (TYPO-02)
  editorialRegular: 'CormorantGaramond_400Regular',
  editorialItalic: 'CormorantGaramond_400Regular_Italic',

  // Geometric sans — body and UI (TYPO-03)
  bodyRegular: 'Raleway_400Regular',
  bodyMedium: 'Raleway_500Medium',
} as const;
```

Update the textStyles export to add these named variants (add them alongside existing entries, do NOT remove existing entries for backward compatibility):
```typescript
// Display variants — Cinzel (TYPO-01, TYPO-04)
displayHero: {
  fontFamily: fontFamilies.displayBold,
  fontSize: 32,
  letterSpacing: 4,
  lineHeight: 38,
} as TextStyle,
displayHeading: {
  fontFamily: fontFamilies.displayRegular,
  fontSize: 22,
  letterSpacing: 2,
  lineHeight: 28,
} as TextStyle,

// Affirmation variants — Cormorant Garamond (TYPO-02, TYPO-04)
affirmation: {
  fontFamily: fontFamilies.editorialRegular,
  fontSize: 24,
  lineHeight: 36,
} as TextStyle,
affirmationItalic: {
  fontFamily: fontFamilies.editorialItalic,
  fontSize: 22,
  lineHeight: 34,
} as TextStyle,

// Body variants — Raleway (TYPO-03, TYPO-04)
body: {
  fontFamily: fontFamilies.bodyRegular,
  fontSize: 15,
  lineHeight: 22,
} as TextStyle,
bodySmall: {
  fontFamily: fontFamilies.bodyRegular,
  fontSize: 14,
  lineHeight: 21,
} as TextStyle,
label: {
  fontFamily: fontFamilies.bodyMedium,
  fontSize: 12,
  letterSpacing: 0.5,
  lineHeight: 16,
} as TextStyle,
button: {
  fontFamily: fontFamilies.bodyMedium,
  fontSize: 15,
  letterSpacing: 0.5,
  lineHeight: 20,
} as TextStyle,
```

CRITICAL anti-pattern: Do NOT combine a named fontFamily with a fontWeight that has no matching loaded variant. For example, do NOT write `fontFamily: fontFamilies.bodyRegular, fontWeight: '700'` — Android renders this as system font (Roboto). Use only the 6 named variants in fontFamilies. No fontWeight in any of the new textStyles entries.

**In index.ts:**
Add `fontFamilies` to the re-exports from typography.ts. Read the current index.ts first, then add the export:
```typescript
export { fontFamilies } from './typography';
```
  </action>
  <verify>
    <automated>cd /Users/joshuabellhome/wavium/wavium && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
    <manual>Run: grep -n "fontFamilies\|displayHero\|affirmation\|Raleway" wavium/src/theme/typography.ts — should show 10+ lines confirming all new entries are present. Confirm no fontWeight appears in the new entries.</manual>
  </verify>
  <done>fontFamilies constant is exported from typography.ts with 6 font variant names. textStyles includes displayHero, displayHeading, affirmation, affirmationItalic, body, bodySmall, label, button variants. fontFamilies is re-exported from index.ts. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
Run from /Users/joshuabellhome/wavium/wavium:
1. `npx tsc --noEmit` — 0 errors
2. `grep "Cinzel_400Regular" app.json` — returns match
3. `grep "fontFamilies" src/theme/typography.ts | head -5` — returns match
4. `grep "displayHero\|affirmation\|bodyMedium" src/theme/typography.ts` — all 3 match
5. `ls node_modules/@expo-google-fonts/cinzel/ | grep .ttf` — shows Cinzel_400Regular.ttf and Cinzel_700Bold.ttf
</verification>

<success_criteria>
- All 3 @expo-google-fonts packages installed in node_modules
- app.json expo-font plugin has 6 font paths, all pointing to existing .ttf files
- fontFamilies constant exported from typography.ts with 6 named variants
- textStyles has new entries: displayHero, displayHeading, affirmation, affirmationItalic, body, bodySmall, label, button
- No fontWeight in any of the 8 new textStyle entries
- No useFonts() call added anywhere in the codebase
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `/Users/joshuabellhome/wavium/.planning/phases/01-token-foundation/01-02-SUMMARY.md`
</output>
