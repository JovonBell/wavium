---
phase: 01-token-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - wavium/src/components/void/NebulaRenderer.tsx
  - wavium/src/components/ui/TimeShiftingBackground.tsx
autonomous: true
requirements:
  - COLR-06

must_haves:
  truths:
    - "NebulaRenderer has no hardcoded hex color constants — uses colors.primaryGradient from useThemeStore"
    - "TimeShiftingBackground has no hardcoded GRADIENTS or AMBIENT_ORBS constants"
    - "The unused interpolateColor import from react-native-reanimated is removed from TimeShiftingBackground"
    - "Both Skia components still render without runtime errors"
  artifacts:
    - path: "wavium/src/components/void/NebulaRenderer.tsx"
      provides: "Skia nebula renderer consuming theme tokens"
      contains: "colors.primaryGradient"
    - path: "wavium/src/components/ui/TimeShiftingBackground.tsx"
      provides: "Skia background renderer consuming theme tokens"
      contains: "useThemeStore"
  key_links:
    - from: "wavium/src/components/void/NebulaRenderer.tsx"
      to: "wavium/src/theme/colors.ts"
      via: "useThemeStore().colors.primaryGradient"
      pattern: "colors\\.primaryGradient"
    - from: "wavium/src/components/ui/TimeShiftingBackground.tsx"
      to: "wavium/src/theme/colors.ts"
      via: "useThemeStore().colors"
      pattern: "colors\\.(background|primaryGradient|primary)"
---

<objective>
Replace hardcoded hex color constants in NebulaRenderer and TimeShiftingBackground with theme token consumption via useThemeStore, and remove the unused interpolateColor import from TimeShiftingBackground — completing the token system's reach into Skia components.

Purpose: After Plan 01 adds primaryGradient to all themes, Skia components must consume it. Hardcoded color constants in Skia files break the single-source-of-truth — a theme token change must propagate automatically, not require hunting across component files.
Output: Both Skia components pull colors from useThemeStore; no local color constants remain.
</objective>

<execution_context>
@/Users/joshuabellhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuabellhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joshuabellhome/wavium/.planning/ROADMAP.md
@/Users/joshuabellhome/wavium/.planning/STATE.md
@/Users/joshuabellhome/wavium/.planning/phases/01-token-foundation/01-RESEARCH.md
@/Users/joshuabellhome/wavium/.planning/phases/01-token-foundation/01-01-SUMMARY.md
@/Users/joshuabellhome/wavium/wavium/src/components/void/NebulaRenderer.tsx
@/Users/joshuabellhome/wavium/wavium/src/components/ui/TimeShiftingBackground.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor NebulaRenderer to consume primaryGradient from theme</name>
  <files>wavium/src/components/void/NebulaRenderer.tsx</files>
  <action>
Read the current NebulaRenderer.tsx file in full first. Then:

1. Identify the hardcoded color block — it will look like a `nebulaColors` useMemo with a switch(timeOfDay) statement returning `{ primary, secondary, tertiary }` hex objects for each time-of-day case.

2. Remove that entire useMemo block (the switch statement and the const nebulaColors declaration).

3. The component already imports useThemeStore (confirmed in research). Add the colors destructure:
```typescript
const { colors } = useThemeStore();
const [nebulaP, nebulaS, nebulaT] = colors.primaryGradient;
```

4. Replace all references to `nebulaColors.primary`, `nebulaColors.secondary`, `nebulaColors.tertiary` throughout the component with `nebulaP`, `nebulaS`, `nebulaT` respectively.

5. If the clouds/particles useMemo array referenced nebulaColors, update its dependency array to reference [nebulaP, nebulaS, nebulaT] instead of [nebulaColors] or [timeOfDay].

IMPORTANT: Do not change any animation logic, canvas dimensions, or rendering structure — only replace the color source. Skia color props expect plain hex strings — colors.primaryGradient entries are hex strings, so no conversion is needed.
  </action>
  <verify>
    <automated>cd /Users/joshuabellhome/wavium/wavium && npx tsc --noEmit 2>&1 | grep "NebulaRenderer" | head -10</automated>
    <manual>Run: grep -n "hardcoded\|switch.*timeOfDay\|nebulaColors" src/components/void/NebulaRenderer.tsx — should return 0 matches. Run: grep -n "primaryGradient" src/components/void/NebulaRenderer.tsx — should return 1+ match.</manual>
  </verify>
  <done>NebulaRenderer has no switch(timeOfDay) color block. It destructures colors.primaryGradient from useThemeStore and uses the 3 stops as cloud colors. TypeScript reports no errors for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TimeShiftingBackground to consume theme tokens and remove dead imports</name>
  <files>wavium/src/components/ui/TimeShiftingBackground.tsx</files>
  <action>
Read the current TimeShiftingBackground.tsx file in full first. Then:

1. Find and REMOVE the `interpolateColor` import from react-native-reanimated (research confirmed it is imported on line ~23 but unused by the Skia canvas). Remove only this import — do not remove other reanimated imports that are actually used (like useAnimatedStyle, withTiming, etc. if present).

2. Find and REMOVE the hardcoded `GRADIENTS` constant (Record<TimeOfDay, [string, string, string]>). This object has the old morning/afternoon/evening/night gradient stops.

3. Find and REMOVE any `AMBIENT_ORBS` constant if present.

4. The component already imports useThemeStore. Add the colors destructure where the removed constants were:
```typescript
const { colors } = useThemeStore();
```

5. Replace the usage of the removed constants:
   - Where `GRADIENTS[timeOfDay]` was used for Skia LinearGradient stops, replace with:
     ```typescript
     const gradient: [string, string, string] = [colors.background, colors.backgroundAlt, colors.surface];
     ```
   - Where GRADIENTS array elements were passed to Skia `<LinearGradient colors={...}>` prop, pass the gradient variable instead.
   - Where ambient orb colors referenced AMBIENT_ORBS[timeOfDay], replace with `colors.primary` or `colors.primaryGradient[0]` as appropriate to the visual intent (the orb accent color).

6. Ensure the Skia canvas still receives valid hex strings for all color props. colors.background, colors.backgroundAlt, colors.surface, and colors.primaryGradient items are all plain hex strings.

IMPORTANT: Do not change animation timings, canvas structure, or any Reanimated SharedValue logic. Only replace color sources and remove the unused import.
  </action>
  <verify>
    <automated>cd /Users/joshuabellhome/wavium/wavium && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
    <manual>Run: grep -n "interpolateColor\|GRADIENTS\|AMBIENT_ORBS" src/components/ui/TimeShiftingBackground.tsx — should return 0 matches. Run: grep -n "primaryGradient\|colors\.background" src/components/ui/TimeShiftingBackground.tsx — should return 1+ matches.</manual>
  </verify>
  <done>TimeShiftingBackground has no GRADIENTS constant, no AMBIENT_ORBS constant, and no interpolateColor import. It reads background and gradient colors from useThemeStore().colors. TypeScript compiles clean across the entire project.</done>
</task>

</tasks>

<verification>
Run from /Users/joshuabellhome/wavium/wavium:
1. `npx tsc --noEmit` — 0 errors across the full project
2. `grep -rn "GRADIENTS\|nebulaColors\|AMBIENT_ORBS" src/components/` — 0 matches
3. `grep -n "interpolateColor" src/components/ui/TimeShiftingBackground.tsx` — 0 matches
4. `grep -n "primaryGradient" src/components/void/NebulaRenderer.tsx src/components/ui/TimeShiftingBackground.tsx` — 1+ matches in each file
</verification>

<success_criteria>
- NebulaRenderer.tsx: no hardcoded color switch/map, imports colors.primaryGradient from useThemeStore
- TimeShiftingBackground.tsx: no GRADIENTS constant, no AMBIENT_ORBS constant, no interpolateColor import
- Both files pull colors from useThemeStore().colors (background, primaryGradient, or primary as appropriate)
- Full project TypeScript compilation: 0 errors
</success_criteria>

<output>
After completion, create `/Users/joshuabellhome/wavium/.planning/phases/01-token-foundation/01-03-SUMMARY.md`
</output>
