---
phase: 03-database-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260202000001_create_subliminals.sql
  - supabase/migrations/20260202000002_create_sessions.sql
  - supabase/migrations/20260202000003_create_mindi_state.sql
autonomous: true

must_haves:
  truths:
    - "subliminals table exists with RLS policies"
    - "sessions table exists with RLS policies"
    - "mindi_state table exists with auto-creation trigger"
  artifacts:
    - path: "supabase/migrations/20260202000001_create_subliminals.sql"
      provides: "subliminals table with user_id FK and RLS"
      contains: "enable row level security"
    - path: "supabase/migrations/20260202000002_create_sessions.sql"
      provides: "sessions table for listening history"
      contains: "enable row level security"
    - path: "supabase/migrations/20260202000003_create_mindi_state.sql"
      provides: "mindi_state table with signup trigger"
      contains: "on_auth_user_created"
  key_links:
    - from: "subliminals.user_id"
      to: "auth.users(id)"
      via: "foreign key"
      pattern: "references auth.users\\(id\\)"
    - from: "mindi_state"
      to: "auth.users"
      via: "trigger on signup"
      pattern: "after insert on auth.users"
---

<objective>
Create Supabase database schema with three core tables and RLS policies for multi-tenant data isolation.

Purpose: Establish the database foundation for user data persistence. Without this schema, no frontend data can be saved to Supabase. RLS policies ensure users can only access their own data.

Output: 3 SQL migration files ready to be applied to Supabase.
</objective>

<execution_context>
@C:\Users\jbell4\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jbell4\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-database-integration/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subliminals table migration</name>
  <files>supabase/migrations/20260202000001_create_subliminals.sql</files>
  <action>
Create supabase/ directory and migrations/ subdirectory if they don't exist.

Create the subliminals table with:
- id: UUID primary key with gen_random_uuid() default
- user_id: UUID foreign key to auth.users(id) with ON DELETE CASCADE
- title: TEXT NOT NULL
- intention: TEXT NOT NULL
- affirmations: TEXT[] NOT NULL (array)
- track: TEXT NOT NULL
- audio_url: TEXT NOT NULL
- created_at: TIMESTAMPTZ DEFAULT now() NOT NULL
- updated_at: TIMESTAMPTZ DEFAULT now() NOT NULL

Enable Row Level Security (CRITICAL).

Create index on user_id for RLS performance.

Create four RLS policies:
- SELECT: using ((select auth.uid()) = user_id)
- INSERT: with check ((select auth.uid()) = user_id)
- UPDATE: using and with check ((select auth.uid()) = user_id)
- DELETE: using ((select auth.uid()) = user_id)

Create updated_at trigger function and trigger.

Use `(select auth.uid())` syntax (wrapped in select) for query plan optimization per Supabase best practices.
  </action>
  <verify>File exists and contains: CREATE TABLE, enable row level security, CREATE POLICY (4 policies), CREATE INDEX</verify>
  <done>subliminals table migration with RLS policies is ready</done>
</task>

<task type="auto">
  <name>Task 2: Create sessions table migration</name>
  <files>supabase/migrations/20260202000002_create_sessions.sql</files>
  <action>
Create the sessions table for tracking listening history:
- id: UUID primary key with gen_random_uuid() default
- user_id: UUID foreign key to auth.users(id) with ON DELETE CASCADE
- subliminal_id: UUID foreign key to subliminals(id) with ON DELETE SET NULL (keep session even if subliminal deleted)
- started_at: TIMESTAMPTZ DEFAULT now() NOT NULL
- ended_at: TIMESTAMPTZ (nullable - null means in progress)
- duration_seconds: INTEGER DEFAULT 0
- completed: BOOLEAN DEFAULT false
- created_at: TIMESTAMPTZ DEFAULT now() NOT NULL

Enable Row Level Security.

Create indexes:
- idx_sessions_user_id on user_id (for RLS)
- idx_sessions_started_at on started_at (for streak queries)

Create three RLS policies (no DELETE - sessions are historical record):
- SELECT: using ((select auth.uid()) = user_id)
- INSERT: with check ((select auth.uid()) = user_id)
- UPDATE: using ((select auth.uid()) = user_id)
  </action>
  <verify>File exists and contains: CREATE TABLE sessions, enable row level security, 2 CREATE INDEX, 3 CREATE POLICY</verify>
  <done>sessions table migration with RLS policies is ready</done>
</task>

<task type="auto">
  <name>Task 3: Create mindi_state table and signup trigger migration</name>
  <files>supabase/migrations/20260202000003_create_mindi_state.sql</files>
  <action>
Create the mindi_state table for character progression:
- id: UUID primary key with gen_random_uuid() default
- user_id: UUID foreign key to auth.users(id) with ON DELETE CASCADE, UNIQUE constraint
- name: TEXT NOT NULL DEFAULT 'Mindi'
- xp: INTEGER NOT NULL DEFAULT 0
- glow_level: INTEGER NOT NULL DEFAULT 1
- total_sessions: INTEGER NOT NULL DEFAULT 0
- total_minutes: INTEGER NOT NULL DEFAULT 0
- created_at: TIMESTAMPTZ DEFAULT now() NOT NULL
- updated_at: TIMESTAMPTZ DEFAULT now() NOT NULL

Enable Row Level Security.

Create unique index on user_id (only one mindi_state per user).

Create RLS policies:
- SELECT: using ((select auth.uid()) = user_id)
- INSERT: with check ((select auth.uid()) = user_id)
- UPDATE: using ((select auth.uid()) = user_id)
(No DELETE - mindi_state is permanent)

Create trigger function create_mindi_state_for_new_user():
- Runs AFTER INSERT on auth.users
- Inserts new row into mindi_state with just user_id
- Use SECURITY DEFINER to bypass RLS during trigger execution

Create trigger on_auth_user_created that fires after insert on auth.users.

Add updated_at trigger (reuse function from subliminals or create if not exists).
  </action>
  <verify>File exists and contains: CREATE TABLE mindi_state, UNIQUE, enable row level security, create_mindi_state_for_new_user, on_auth_user_created trigger</verify>
  <done>mindi_state table with auto-creation trigger is ready</done>
</task>

</tasks>

<verification>
All SQL files:
1. Have valid PostgreSQL syntax
2. Enable RLS on every table
3. Have at least SELECT and INSERT policies
4. Use (select auth.uid()) pattern for optimization
5. Have indexes on user_id columns
</verification>

<success_criteria>
- 3 migration files exist in supabase/migrations/
- Each file is syntactically valid SQL
- All tables have RLS enabled
- All tables have appropriate indexes
- mindi_state has signup trigger
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-integration/03-01-SUMMARY.md`
</output>
