---
phase: 03-database-integration
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - wavium/src/services/sessionService.ts
  - wavium/src/services/mindiService.ts
autonomous: true

must_haves:
  truths:
    - "Session can be started with subliminal ID"
    - "Session can be ended with duration and completion status"
    - "User streak can be fetched via RPC"
    - "Mindi state can be fetched from Supabase"
    - "XP can be added atomically via RPC"
  artifacts:
    - path: "wavium/src/services/sessionService.ts"
      provides: "Session recording operations"
      exports: ["startSession", "endSession", "getUserStreak", "getSessionHistory"]
    - path: "wavium/src/services/mindiService.ts"
      provides: "Mindi state persistence operations"
      exports: ["getMindiState", "updateMindiState", "updateMindiName", "addSessionXP"]
  key_links:
    - from: "wavium/src/services/sessionService.ts"
      to: "supabase.from('sessions')"
      via: "Supabase client"
      pattern: "supabase\\.from\\(['\"]sessions['\"]\\)"
    - from: "wavium/src/services/sessionService.ts"
      to: "get_user_streak"
      via: "Supabase RPC"
      pattern: "supabase\\.rpc\\(['\"]get_user_streak['\"]"
    - from: "wavium/src/services/mindiService.ts"
      to: "supabase.from('mindi_state')"
      via: "Supabase client"
      pattern: "supabase\\.from\\(['\"]mindi_state['\"]\\)"
    - from: "wavium/src/services/mindiService.ts"
      to: "add_session_xp"
      via: "Supabase RPC"
      pattern: "supabase\\.rpc\\(['\"]add_session_xp['\"]"
---

<objective>
Create session and Mindi state service layers for recording listening history and character progression.

Purpose: Sessions must be recorded for streak calculation. Mindi state must persist to Supabase so XP, glow level, and total sessions sync across devices. Service layer abstracts Supabase queries from UI components.

Output: 2 service files with complete CRUD operations.
</objective>

<execution_context>
@C:\Users\jbell4\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jbell4\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-database-integration/03-RESEARCH.md
@.planning/phases/03-database-integration/03-01-SUMMARY.md
@wavium/src/lib/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session service</name>
  <files>wavium/src/services/sessionService.ts</files>
  <action>
Create service file with module-level variable to track active session:
let activeSessionId: string | null = null

Export functions:

startSession(subliminalId: string): Promise<Session>
- Get current user, throw if not authenticated
- Insert into sessions: user_id, subliminal_id, started_at (now())
- Use .select().single() to get created row
- Store id in activeSessionId
- Return Session type

endSession(completed: boolean, durationSeconds: number): Promise<void>
- If no activeSessionId, return early (no-op)
- Update session where id = activeSessionId:
  - ended_at = now()
  - duration_seconds = durationSeconds
  - completed = completed
- Reset activeSessionId to null

getUserStreak(): Promise<StreakData>
- Get current user, throw if not authenticated
- Call supabase.rpc('get_user_streak', { p_user_id: user.id })
- Return { currentStreak, longestStreak, lastSessionDate } (transform snake_case)

getSessionHistory(limit = 20): Promise<Session[]>
- Select from sessions ordered by started_at DESC, limit
- Include subliminal data via join or separate query
- RLS filters to current user automatically

Define types:
- Session: id, subliminalId, startedAt, endedAt, durationSeconds, completed
- StreakData: currentStreak, longestStreak, lastSessionDate

Import supabase from @/lib/supabase.
  </action>
  <verify>TypeScript compiles: cd wavium && npx tsc --noEmit</verify>
  <done>Session service exports startSession, endSession, getUserStreak, getSessionHistory</done>
</task>

<task type="auto">
  <name>Task 2: Create Mindi state service</name>
  <files>wavium/src/services/mindiService.ts</files>
  <action>
Create service file that exports:

getMindiState(): Promise<MindiState | null>
- Get current user, return null if not authenticated
- Select from mindi_state where user_id = user.id, .single()
- Handle PGRST116 error (no rows) gracefully - return null (trigger may not have fired yet)
- Transform snake_case to camelCase
- Return MindiState or null

updateMindiName(name: string): Promise<void>
- Get current user, throw if not authenticated
- Update mindi_state set name where user_id = user.id
- Throw on error

updateMindiState(updates: Partial<MindiStateUpdates>): Promise<void>
- Get current user, throw if not authenticated
- Build dbUpdates object, mapping camelCase to snake_case:
  - xp -> xp
  - glowLevel -> glow_level
  - totalSessions -> total_sessions
  - totalMinutes -> total_minutes
- Update mindi_state where user_id = user.id
- Throw on error

addSessionXP(durationMinutes: number): Promise<void>
- Calculate XP: 10 (base) + durationMinutes
- Get current user, return early if not authenticated
- Call supabase.rpc('add_session_xp', { p_user_id: user.id, p_xp: xpGained, p_minutes: durationMinutes })
- Log error but don't throw (XP is non-critical)

Define types:
- MindiState: name, xp, glowLevel, totalSessions, totalMinutes
- MindiStateUpdates: Omit<MindiState, 'name'> (name updated separately)

Import supabase from @/lib/supabase.
  </action>
  <verify>TypeScript compiles: cd wavium && npx tsc --noEmit</verify>
  <done>Mindi service exports getMindiState, updateMindiName, updateMindiState, addSessionXP</done>
</task>

</tasks>

<verification>
1. wavium/src/services/sessionService.ts exports 4 functions
2. wavium/src/services/mindiService.ts exports 4 functions
3. TypeScript compilation passes
4. Both services import from @/lib/supabase
</verification>

<success_criteria>
- sessionService.ts exists with startSession, endSession, getUserStreak, getSessionHistory
- mindiService.ts exists with getMindiState, updateMindiName, updateMindiState, addSessionXP
- TypeScript compiles without errors
- Services use RPC for streak and XP calculations
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-integration/03-03-SUMMARY.md`
</output>
