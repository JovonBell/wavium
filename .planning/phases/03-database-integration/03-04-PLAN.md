---
phase: 03-database-integration
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - wavium/src/stores/useMindiStore.ts
  - wavium/src/hooks/useMindiSync.ts
  - wavium/app/_layout.tsx
  - wavium/src/components/StreakDisplay.tsx
autonomous: true

must_haves:
  truths:
    - "useMindiStore syncs with Supabase on auth state change"
    - "saveSubliminal persists to Supabase"
    - "deleteSubliminal removes from Supabase"
    - "Mindi state loads from Supabase on login"
    - "Session completion triggers XP update"
    - "User can see their current streak in the UI"
  artifacts:
    - path: "wavium/src/stores/useMindiStore.ts"
      provides: "Store with Supabase integration"
      contains: "subliminalService"
    - path: "wavium/src/hooks/useMindiSync.ts"
      provides: "Sync hook for Mindi state"
      exports: ["useMindiSync"]
    - path: "wavium/app/_layout.tsx"
      provides: "Root layout with sync hook wired"
      contains: "useMindiSync"
    - path: "wavium/src/components/StreakDisplay.tsx"
      provides: "Streak display component"
      exports: ["StreakDisplay"]
  key_links:
    - from: "wavium/src/stores/useMindiStore.ts"
      to: "wavium/src/services/subliminalService.ts"
      via: "service import"
      pattern: "import.*subliminalService"
    - from: "wavium/src/hooks/useMindiSync.ts"
      to: "wavium/src/services/mindiService.ts"
      via: "service import"
      pattern: "import.*mindiService"
    - from: "wavium/app/_layout.tsx"
      to: "wavium/src/hooks/useMindiSync.ts"
      via: "hook call in RootNavigator"
      pattern: "useMindiSync\\(\\)"
---

<objective>
Integrate Zustand store with Supabase services so subliminals and Mindi state sync with the database. Wire useMindiSync into the app root and display streak in UI.

Purpose: The existing useMindiStore uses AsyncStorage for persistence. This plan updates it to use Supabase as the source of truth while keeping local state for UI responsiveness. A new sync hook handles loading Mindi state on login. Streak display completes the DB-07 requirement.

Output: Updated store with Supabase integration, new sync hook wired into app root, streak UI component.
</objective>

<execution_context>
@C:\Users\jbell4\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jbell4\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-database-integration/03-RESEARCH.md
@.planning/phases/03-database-integration/03-02-SUMMARY.md
@.planning/phases/03-database-integration/03-03-SUMMARY.md
@wavium/src/stores/useMindiStore.ts
@wavium/src/services/subliminalService.ts
@wavium/src/services/mindiService.ts
@wavium/src/hooks/useAuth.ts
@wavium/app/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useMindiStore to use Supabase for subliminals</name>
  <files>wavium/src/stores/useMindiStore.ts</files>
  <action>
Update the store to integrate with Supabase:

1. Keep persist middleware but REMOVE subliminals from persisted state:
   - The subliminals array should NOT be persisted locally anymore
   - Supabase is now the source of truth
   - Only persist: name, userId, currentState, creation (local UI state)

2. Update partialize option in persist config:
   partialize: (state) => ({
     name: state.name,
     userId: state.userId,
     currentState: state.currentState,
     creation: state.creation,
   })

3. Add async action: saveSubliminalToDb(title: string, audioUrl: string): Promise<Subliminal>
   - Import saveSubliminal from @/services/subliminalService
   - Get creation state from store
   - Call saveSubliminal service with { title, intention, affirmations, track, audioUrl }
   - Clear creation state after successful save
   - Return the created subliminal
   - Let errors propagate (UI will catch)

4. Add async action: deleteSubliminalFromDb(id: string): Promise<void>
   - Import deleteSubliminal from @/services/subliminalService
   - Call service function
   - Let errors propagate

5. Add action: setSubliminals(subliminals: Subliminal[]): void
   - Directly set subliminals array
   - Used by sync hook to populate from Supabase

6. Keep existing synchronous saveSubliminal for backwards compatibility but mark deprecated:
   - Add JSDoc comment: @deprecated Use saveSubliminalToDb instead
   - Keep implementation (generates local ID) for offline fallback

7. Update getSubliminal to work with current subliminals array (no change needed).

Import subliminalService at top of file.
Keep all existing types (MindiState, SoundTrack, Subliminal, etc).
  </action>
  <verify>TypeScript compiles: cd wavium && npx tsc --noEmit</verify>
  <done>useMindiStore has saveSubliminalToDb, deleteSubliminalFromDb, setSubliminals actions</done>
</task>

<task type="auto">
  <name>Task 2: Create useMindiSync hook for auth-aware state sync</name>
  <files>wavium/src/hooks/useMindiSync.ts</files>
  <action>
Create a hook that syncs Mindi state and subliminals when user logs in:

useMindiSync(): { loading: boolean, error: Error | null }

Implementation:
1. Import useAuth from @/hooks/useAuth
2. Import useMindiStore (get setSubliminals, set store methods)
3. Import getMindiState, addSessionXP from @/services/mindiService
4. Import getUserStreak from @/services/sessionService
5. Import useSyncedLibrary from @/hooks/useSyncedLibrary
6. useState for loading, error

7. Get { user } from useAuth()

8. Use useSyncedLibrary hook: const { subliminals, loading: libraryLoading } = useSyncedLibrary(user?.id ?? null)

9. useEffect on subliminals change:
   - Call setSubliminals(subliminals) to update store

10. useEffect on user?.id change:
   - If no user, return (logged out state)
   - setLoading(true)
   - Fetch Mindi state: const mindiState = await getMindiState()
   - If mindiState exists:
     - Update store: setName(mindiState.name)
     - Store XP, glow level in Zustand (add to store if needed) or keep in component state
   - Fetch streak data: const streakData = await getUserStreak()
   - Update store with streak data
   - setLoading(false)
   - Catch errors, set error state

11. Return { loading: loading || libraryLoading, error }

This hook should be used at app root level (inside AuthProvider) to ensure sync happens on login.

Export useMindiSync.
  </action>
  <verify>TypeScript compiles: cd wavium && npx tsc --noEmit</verify>
  <done>useMindiSync hook syncs Mindi state and subliminals on auth change</done>
</task>

<task type="auto">
  <name>Task 3: Wire useMindiSync into app root layout</name>
  <files>wavium/app/_layout.tsx</files>
  <action>
Update the root layout to call useMindiSync inside RootNavigator (which is already inside AuthProvider).

1. Import useMindiSync from @/hooks/useMindiSync

2. Inside RootNavigator function, call the hook:
   const { loading: syncLoading, error: syncError } = useMindiSync();

3. Optionally log syncError if present (for debugging):
   useEffect(() => {
     if (syncError) console.warn('Mindi sync error:', syncError);
   }, [syncError]);

4. The hook handles all the sync logic internally - no additional wiring needed.
   The sync happens automatically when user logs in (useAuth provides the user).

DO NOT modify the loading/rendering logic - the existing isHydrated and loading checks are sufficient.
The sync hook runs in the background and updates the store, which triggers re-renders as needed.
  </action>
  <verify>TypeScript compiles: cd wavium && npx tsc --noEmit</verify>
  <done>useMindiSync is called in RootNavigator, syncing Mindi state on auth changes</done>
</task>

<task type="auto">
  <name>Task 4: Add evolution state to useMindiStore and create StreakDisplay component</name>
  <files>
    wavium/src/stores/useMindiStore.ts
    wavium/src/components/StreakDisplay.tsx
  </files>
  <action>
Part A: Add evolution-related state to the store (extends Task 1 changes):

1. Add to state interface:
   - xp: number (default 0)
   - glowLevel: number (default 1)
   - totalSessions: number (default 0)
   - totalMinutes: number (default 0)
   - currentStreak: number (default 0)
   - longestStreak: number (default 0)

2. Add to initial state with defaults.

3. Add action: setEvolutionState(state: EvolutionState): void
   - Sets xp, glowLevel, totalSessions, totalMinutes

4. Add action: setStreakData(data: StreakData): void
   - Sets currentStreak, longestStreak

5. Add these to partialize (persist locally for offline access):
   - xp, glowLevel, totalSessions, totalMinutes, currentStreak, longestStreak

6. Define types:
   - EvolutionState: { xp, glowLevel, totalSessions, totalMinutes }
   - StreakData: { currentStreak, longestStreak }

Part B: Create StreakDisplay component:

Create wavium/src/components/StreakDisplay.tsx that shows the user's current streak:

1. Import useMindiStore to get currentStreak, longestStreak
2. Display streak with fire emoji and count
3. Style with Tailwind/NativeWind classes
4. Show "0" when no streak (not hidden)
5. Optionally show longest streak as smaller text below

Example structure:
```tsx
export function StreakDisplay() {
  const { currentStreak, longestStreak } = useMindiStore();

  return (
    <View className="flex-row items-center gap-1">
      <Text className="text-orange-500 text-lg">FIRE_EMOJI</Text>
      <Text className="text-white font-bold text-lg">{currentStreak}</Text>
      {longestStreak > currentStreak && (
        <Text className="text-gray-400 text-sm ml-2">Best: {longestStreak}</Text>
      )}
    </View>
  );
}
```

This component can be placed in the home screen or Mindi stats area.
  </action>
  <verify>TypeScript compiles: cd wavium && npx tsc --noEmit</verify>
  <done>useMindiStore has evolution state (xp, glowLevel, streaks) with persistence, StreakDisplay component created</done>
</task>

</tasks>

<verification>
1. useMindiStore exports saveSubliminalToDb, deleteSubliminalFromDb
2. useMindiStore has evolution state fields
3. useMindiSync hook exists and syncs on auth change
4. useMindiSync is called in app/_layout.tsx RootNavigator
5. StreakDisplay component exists and shows streak
6. TypeScript compilation passes
7. subliminals NOT in persisted state (Supabase is source of truth)
</verification>

<success_criteria>
- useMindiStore.ts updated with Supabase integration
- useMindiSync.ts created with auth-aware sync
- useMindiSync wired into wavium/app/_layout.tsx
- Evolution state (xp, glowLevel, streaks) persisted locally
- Subliminals removed from local persistence
- StreakDisplay component shows current streak
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-integration/03-04-SUMMARY.md`
</output>
