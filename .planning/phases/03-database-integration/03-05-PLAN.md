---
phase: 03-database-integration
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Migrations apply successfully to Supabase"
    - "User can save subliminal and see it in library"
    - "User can delete subliminal from library"
    - "Mindi state loads on login"
    - "User can see their streak in the UI"
    - "Changes sync across devices"
  artifacts: []
  key_links: []
---

<objective>
Apply migrations to Supabase and verify end-to-end database integration works.

Purpose: Ensure all schema migrations apply correctly and the frontend can perform all CRUD operations against the live database with proper RLS enforcement.

Output: Verified working database integration.
</objective>

<execution_context>
@C:\Users\jbell4\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jbell4\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-database-integration/03-RESEARCH.md
@.planning/phases/03-database-integration/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migrations via Supabase SQL Editor</name>
  <files></files>
  <action>
Since Supabase CLI requires project linking and may not be set up, provide instructions for applying migrations via SQL Editor.

Create a combined migration file for easy copy-paste:

1. Read all 5 migration files in order
2. Combine into a single SQL script with comments separating each migration
3. Output the combined SQL for the user to paste into Supabase SQL Editor

Alternative: If supabase CLI is available:
- npx supabase db push (if linked)
- Or: npx supabase login && npx supabase link --project-ref <ref> && npx supabase db push

For this task, generate a single combined SQL file at:
supabase/combined_migrations.sql

This file should:
- Include all 5 migrations in order
- Have clear section comments
- Be ready for copy-paste into Supabase Dashboard -> SQL Editor
  </action>
  <verify>Combined SQL file exists at supabase/combined_migrations.sql</verify>
  <done>Combined migration SQL ready for manual application</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply migrations to Supabase</name>
  <action>
Apply the database migrations to your Supabase project.
  </action>
  <instructions>
1. Open Supabase Dashboard: https://supabase.com/dashboard
2. Select your Wavium project
3. Go to SQL Editor (left sidebar)
4. Open the file: supabase/combined_migrations.sql
5. Copy the entire contents
6. Paste into SQL Editor
7. Click "Run" (or Ctrl+Enter)
8. Verify no errors appear

Expected result:
- "Success. No rows returned" for each statement
- Tables visible in Table Editor: subliminals, sessions, mindi_state
- Functions visible in Database -> Functions: get_user_streak, add_session_xp
  </instructions>
  <resume-signal>Type "migrations applied" when complete, or describe any errors</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify database integration end-to-end</name>
  <what-built>
Complete database integration with:
- 3 tables: subliminals, sessions, mindi_state
- 2 RPC functions: get_user_streak, add_session_xp
- RLS policies on all tables
- Service layer with CRUD operations
- Realtime sync hook
- Store integration
- Streak display component
  </what-built>
  <how-to-verify>
1. Start the app: cd wavium && npx expo start
2. Log in with a test account (or create one)
3. Verify Mindi state loads (check console for any errors)
4. Check that streak displays in UI (even if 0)
5. Navigate to where subliminals are displayed (library/home)
6. If there's a way to create a subliminal, try it:
   - The subliminal should appear in the list
   - Check Supabase Table Editor - row should exist in subliminals table
7. Open app on another device/simulator with same account:
   - Subliminal should appear (cross-device sync)
8. Delete the subliminal:
   - Should disappear from list
   - Should disappear from Supabase table

If no UI exists yet for creating subliminals, verify via:
- Console: no errors on login
- Supabase: mindi_state row created for user
- TypeScript: npx tsc --noEmit passes
  </how-to-verify>
  <resume-signal>Type "verified" if working, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All migrations applied to Supabase without errors
2. Tables exist in Supabase Table Editor
3. RLS policies active (visible in Authentication -> Policies)
4. RPC functions callable
5. Frontend can read/write data
6. Streak displays in UI
</verification>

<success_criteria>
- Migrations applied successfully
- No TypeScript errors
- CRUD operations work against live database
- RLS enforces user isolation
- Streak visible in UI
- Realtime updates work across devices (if testable)
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-integration/03-05-SUMMARY.md`
</output>
